/** Admin-supplied portrait for a manager on the Ranking page (external URL — Spark plan). */
export interface ManagerImage {
  id: string; // slug of the manager name
  manager: string;
  imageUrl: string;
}
